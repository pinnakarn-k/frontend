import { Stack, Typography } from '@mui/material';

export function PreviewPage() {
    return (
        <Stack spacing={3}>
            <Typography variant="h4">HD Preview</Typography>

            <Typography>
                Document preview will be implemented here.
            </Typography>
        </Stack>
    );
}